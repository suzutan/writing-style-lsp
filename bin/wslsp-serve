#!/usr/bin/env python3
"""stdio LSP サーバーの起動ラッパー。cwd に依存せず wslsp パッケージを解決する。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from wslsp.server import main  # noqa: E402

sys.exit(main())
