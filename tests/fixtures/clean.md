# 検証用フィクスチャ（違反なし）

このツールは Markdown の文章規律を検査する。検索エンジンの実装用語は比喩ではないため対象外となる。設定手順を短く保ち、判断は明示する。

課題の詳細は https://github.com/suzutan/writing-style-lsp/issues/1 を参照。関連ノートは [[bad]] にまとめた。

| 旧 | 新 |
| --- | --- |
| a | b |

```bash
grep -n "本記事では" docs/*.md  # コードブロック内は検査対象外
```
